---
import BaseLayout from '../layouts/BaseLayout.astro';
import TermBlock from '../components/TermBlock.astro';
import FeedList from '../components/FeedList.astro';
import ProfilePhoto from '../components/ProfilePhoto.astro';
import { getCollection } from 'astro:content';
import { site } from '../data/site';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const projects = (await getCollection('projects'))
  .sort((a, b) => b.data.year - a.data.year)
  .slice(0, 3);

const recent = posts.slice(0, 4);
const fmt = (d: Date) => d.toLocaleDateString('es-ES', { month: 'short', year: 'numeric' });
---

<BaseLayout path="/">
  <section class="hero">
    <ProfilePhoto />
    <div>
      <h1 class="hero__name">{site.name}</h1>
      <p class="hero__role">{site.role}</p>
      <p class="hero__bio">{site.bio}</p>
      <a class="hero__link" href="/sobre">sobre mí →</a>
    </div>
  </section>

  <TermBlock cmd="ls blog/">
    <FeedList
      items={recent.map((p) => ({
        href: `/blog/${p.id}`,
        title: p.data.title,
        meta: fmt(p.data.pubDate),
      }))}
    />
    <a class="mt-link" href="/blog">ver todos los artículos →</a>
  </TermBlock>

  <TermBlock cmd="ls proyectos/">
    <FeedList
      items={projects.map((p) => ({
        href: `/proyectos/${p.id}`,
        title: p.data.title,
        meta: String(p.data.year),
      }))}
    />
    <a class="mt-link" href="/proyectos">ver proyectos →</a>
  </TermBlock>
</BaseLayout>
