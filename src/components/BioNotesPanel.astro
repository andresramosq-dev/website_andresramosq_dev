---
import { site } from '../data/site';

const noteRank = ['soft', 'primary', 'soft'] as const;
---

<div class="bio-notes">
	{site.bioPersonal.map((line, i) => (
		<p class:list={['bio-notes__line', `bio-notes__line--${noteRank[i] ?? 'soft'}`]}>
			{line}
		</p>
	))}
</div>
