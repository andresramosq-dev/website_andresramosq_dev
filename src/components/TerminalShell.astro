---
import { site } from '../data/site';

interface Props {
	segment?: string;
}

const { segment = '~' } = Astro.props;
---

<div class="term">
	<div class="term__bar">
		<a href="/">{site.domain}</a>
		{' — bash — '}
		<span class="h">{site.user}@{site.domain}</span>:{segment}
	</div>
	<slot />
</div>
