// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	redirects: {
		'/': '/andres-ramos',
		'/sobre': '/andres-ramos',
		'/andres': '/andres-ramos',
	},
});
